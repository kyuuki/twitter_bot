- content_for :title
  | スケジュール一覧

.box
  .box-header.with-border
    .pull-right
      = link_to new_schedule_path, { class: "btn btn-default btn-sm" } do
        i.fa.fa-plus
        | &nbsp; 新規登録

  .box-body.table-responsive

    table.table.table-bordered.table-hover
      thead
        tr
          /th.col-sm-1 ID
          /th.col-sm-2 = t('activerecord.attributes.schedule.category')
          th = t('activerecord.attributes.schedule.post_weekday')
          th = t('activerecord.attributes.schedule.post_time')
          th style="width: 140px;" アクション
      tbody
        == render @schedules

  .box-footer
    .pull-right
      = link_to new_schedule_path, { class: "btn btn-default btn-sm" } do
        i.fa.fa-plus
        | &nbsp; 新規登録

